export default function PropertyCard({key, property}: {key: string, property: any}) {
    return (
        <div className="flex flex-col items-center justify-center h-screen">
            <h1 className="text-4xl font-bold mb-4">Property Card Component</h1>
        </div>
    );
}